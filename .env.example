PORT=8080 # 后端暴露的端口
CORS_ALLOWED_ORIGINS=http://localhost:3000 # 前端暴露的端口
DATABASE_DRIVER=mysql # 驱动类型 mysql/sqlite/postgres sqlite和postgres没测试过
DATABASE_DSN=mysql://user:password@localhost:3306/auralis?sslmode=disable # 数据库DSN
# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

JWT_SECRET=protective # JWT密钥

AGENT_REVIEW_ENABLED=false # 是否需要管理员审核新创建的agent

LLM_API_KEY=your-api-key # 七牛云大模型的api_key
LLM_BASE_URL=https://openai.qiniu.com/v1 # 七牛云大模型的API基础URL
LLM_MODEL_ID=gpt-oss-120b # 默认使用的大模型ID 代码逻辑中有专门的枚举值选择

# 上下文记忆相关
LLM_MEMORY_RECENT_LIMIT=12 # 在总结记忆时考虑的最近消息数量
LLM_MEMORY_SUMMARY_THRESHOLD=6 # 触发总结的消息数量阈值
LLM_MEMORY_SUMMARY_MAX_CHARS=800 # 总结记忆的最大字符数
LLM_MEMORY_SUMMARY_WINDOW=40 # 用于总结的消息窗口大小
# Optional custom prompt for summarization (leave empty to use default)
LLM_MEMORY_SUMMARY_PROMPT=



MINIO_ENDPOINT=localhost:9000          # MinIO API 地址（host:port）
MINIO_ACCESS_KEY=your-access-key       # MinIO Access Key 也就是 username
MINIO_SECRET_KEY=your-secret-key       # MinIO Secret Key 也就是 password
MINIO_BUCKET=auralis-agents            # 用于存储头像的 bucket 名称，可自定义 （不要乱改）
MINIO_USE_SSL=false                    # 如果是 https 改为 true
MINIO_PUBLIC_URL=http://localhost:9001 # MinIO 访问相关挂载的端口

LIVE2D_STORAGE_DIR=./data/live2d         # Live2D 模型归档的本地存储目录

# Text-to-Speech (Qiniu OpenAI)
TTS_QINIU_API_BASE_URL=https://openai.qiniu.com/v1
TTS_QINIU_API_BACKUP_URL=https://api.qnaigc.com/v1
TTS_QINIU_API_KEY=
TTS_QINIU_MODEL_ID=tts

# Text-to-Speech (Aliyun CosyVoice)
COSYVOICE_API_KEY= # 这是百炼大模型的 apikey 不是阿里云服务的 access key
COSYVOICE_MODEL=cosyvoice-v1 # 建议选 v1 ,v2 v3 对接的不好
COSYVOICE_DEFAULT_VOICE=longxiaobai-v1 # 默认的音色
COSYVOICE_SAMPLE_RATE=22050 # 采样率
COSYVOICE_FORMAT=mp3 # 音频格式
COSYVOICE_WS_URL=wss://dashscope.aliyuncs.com/api-ws/v1/inference # WebSocket 连接地址
COSYVOICE_WORKSPACE= # 工作空间 ID 建议别设置需要工作空间的 api_key 因为没测试过
COSYVOICE_DATA_INSPECTION=enable  # 是否开启数据安全检测 enable/disable
COSYVOICE_VOLUME=50 # 音量 0-100 建议 50

# Admin request notifications （这部分是成为管理员给我发通知的）
ADMIN_REQUEST_RECIPIENT_EMAIL=3411596361@qq.com
ADMIN_REQUEST_SMTP_HOST=smtp.qq.com
ADMIN_REQUEST_SMTP_PORT=587
ADMIN_REQUEST_SMTP_USERNAME=3411596361@qq.com
ADMIN_REQUEST_SMTP_PASSWORD=krupytjbhfovcjdi
ADMIN_REQUEST_MAIL_FROM=3411596361@qq.com
ADMIN_REQUEST_MAIL_SUBJECT=New admin request notification

# Knowledge base / embeddings
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_VECTOR_DIM=1536

# Aliyun DashScope OpenAI-compatible settings （不建议改）
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_EMBEDDING_MODEL=text-embedding-v4

# Generic overrides / advanced tuning
EMBEDDING_API_KEY= #就用cosyvoice的key就可以（除了这个字段都不建议改）
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL_ID=text-embedding-v4
EMBEDDING_VECTOR_DIM=1536
EMBEDDING_DIMENSIONS=1536
EMBEDDING_INPUT_TYPE=
EMBEDDING_TEXT_TYPE=document
EMBEDDING_OUTPUT_TYPE=
EMBEDDING_INSTRUCT=
KNOWLEDGE_CHUNK_MAX_CHARS=800
KNOWLEDGE_CHUNK_MIN_CHARS=400
